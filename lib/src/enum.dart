enum NetState { UNINITIALIZED, LOADING, SUCCESS, ERROR }

enum ConnectivityStatus { WiFi, Cellular, Offline }

enum UIDirection { LTR, RTL }

enum OrderShipType { SHIP, BUY_AND_SHIP }

enum ActivityResult {
  RESULT_OK,
  RESULT_CANCELED,
}

enum OrderPaymentStatus{
  PENDING,
  READY,
  PAID
}
